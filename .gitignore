/target
Cargo.lock
# apidoc/ snapshot runner: its target dir is ignored, its lockfile is
# committed (negation must follow the global Cargo.lock ignore above).
apidoc/target/
!apidoc/Cargo.lock

# Downloaded corpus and comparison outputs
/corpus/
/comparison_outputs/
benchmark_results.csv

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
tests/benchmark_tracking/results/*.json
perf.data*
perf_*.data
*.profile.json
*_profile.json

# main-with-lockfile agent coordination
.workongoing
